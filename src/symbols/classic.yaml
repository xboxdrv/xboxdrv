---

- name: 'classic'
  aliases: ['nintendo']
  abs:
    - name: 'x1'
      provides: ['gamepad.x1']
      
    - name: 'y1'
      provides: ['gamepad.y1']
        
    - name: 'x2'
      provides: ['gamepad.x2']

    - name: 'y2'
      provides: ['gamepad.y2']

  key:
    - name:     'a'
      provides: ['gamepad.b']

    - name:     'b'
      provides: ['gamepad.a']

    - name:     'x'
      provides: ['gamepad.y']

    - name:     'y'
      provides: ['gamepad.x']

    - name:    'minus'
      provides: ['gamepad.select']

    - name:    'home'
      provides: ['gamepad.guide']

    - name:    'plus'
      provides: ['gamepad.start']

...
